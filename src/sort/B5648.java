package sort;import java.util.*;public class B5648 {    // 첫 줄의 첫 숫자는 N이고 그 뒤로 N개 만큼의 숫자가 주어진다.    // 입력방식이 Scanner가 편할 것 같아서 BufferedReader 대신 사용했다.    // StringBuilder의 reverse()를 이용해서 주어진 문자열을 뒤집고 Long.parseLong()을 통해 "0"을 제거했다.    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        long N = Long.parseLong(sc.next());        long[] arr = new long[(int) N];        StringBuilder sb;        for (int i = 0; i < (int) N; i++) {            String number = sc.next();            sb = new StringBuilder(number);            sb.reverse();            arr[i] = Long.parseLong(sb.toString());        }        Arrays.sort(arr);        Arrays.stream(arr).forEach(System.out::println);    }}