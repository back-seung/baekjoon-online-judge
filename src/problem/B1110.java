package problem;import java.util.Scanner;public class B1110 {    public static void main(String[] args) {        // 입력 Scanner        Scanner sc = new Scanner(System.in);        //초기값        int intValue = sc.nextInt();        int count = 0; // 카운팅 스탈ㅆ        String defaultV = "";        if (intValue > 10) {            defaultV = Integer.toString(intValue);        } else {            defaultV = "0" + Integer.toString(intValue);        }        while (true) {            int a = defaultV.charAt(0) - '0'; // 10자리            int b = defaultV.charAt(1) - '0';// 1자리            int c = a + b; // 두개 더한 값            if (c > 10) {                defaultV = Integer.toString(b) + Integer.toString(c % 10);            } else {                defaultV = Integer.toString(b) + Integer.toString(c);            }            if (intValue == Integer.parseInt(defaultV)) {                count++;                System.out.println(count);                break;            } else {                count++;            }        }    }}