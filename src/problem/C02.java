import java.util.*;public class C02 {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int N = sc.nextInt(); // 테스트 케이스 개수        ArrayList<String> arr = new ArrayList<String>(N); // 사용자 입력값을 받는 배열 선언        for (int i = 0; i < N; i++) { // 테스트 케이스 개수만큼 배열 초기화            String input = sc.next(); // 입력값            String checkInput = input.replaceAll("[^0-9]", ""); // 정규식을 통해 숫자 추출            if (checkInput.length() < 5) { // 숫자로 추출된 String의 길이가 5보다 작을때만 배열에 삽입.                arr.add(input.charAt(input.length() - 1) + input); // input의 제일 끝 문자를 가장 앞에 붙혀준 뒤, arr에 add()            }        }        Collections.sort(arr); // 정렬        String[] result = new String[arr.size()]; // arr배열을 오름차순으로 정렬한 값으로 초기화 될 배열        for (int i = 0; i < result.length; i++) {            result[i] = arr.get(i).substring(1, arr.get(i).length()); // arr의 각 인덱스에서 맨 앞에 char 제거 후 삽입        }        for (String item : result) { // 출력            System.out.println(item);        }    }}