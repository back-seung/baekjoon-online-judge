import java.util.*;public class Exam_01 {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        int arrSize = sc.nextInt(); // 입력받을 배열의 크기, 1 ~10        ArrayList<Integer> arr = new ArrayList<>(arrSize); // 입력 배열 선언        ArrayList<Integer> delArr = new ArrayList<>(arr); // 중복 제거 배열 선언        for (int i = 0; i < arrSize; i++) { // 배열 크기에 따른 값 입력 받기            arr.add(sc.nextInt());        }        arr.sort(Comparator.naturalOrder()); // 배열 오름차순        for (int i = 1; i < arr.size(); i++) { // 중복 제거 배열에 중복 요소 삽입            if (arr.get(i) == arr.get(i - 1)) {                delArr.add(arr.get(i));            }        }        arr.removeAll(delArr); // 입력 배열 중 중복 배열의 원소를 갖는 값 전체 삭제        if (arr.size() != 0) {  // 배열에 중복이 아닌 값이 있을 때,            for (int i = 0; i < arr.size(); i++) {                System.out.print(arr.get(i)+ ", "); // 배열 출력            }        } else { // 배열의 전체가 중복일 때 ( size == 0 )            System.out.println(-1); // -1 출력        }    }}