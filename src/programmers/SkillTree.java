package programmers;public class SkillTree {    public int solution(String skill, String[] skill_trees) {        // String skill = 유저가 스킬을 배울 순서        // String[] skill_trees = 유저들이 만든 스킬트리 배열        // 가능한 스킬트리 개수        int answer = 0;        // 선행 스킬이 하나면 모든 케이스든 무조건 가능함        if (skill.length() == 1) return skill_trees.length;        // pattern = skill을 제외한 값들 Regex        String pattern = "[^" + skill + "]";        for (String st : skill_trees) {            // skill을 제외한 값들을 모두 ""로 치환            st = st.replaceAll(pattern, "");            System.out.println(st);            // 치환된 값에 skill이 포함되어있지 않은 경우는 올바른 스킬트리로 판단            if (st.equals("")) {                answer++;                continue;            }            for (int i = 1; i <= skill.length(); i++) {                // 입출력 예를 기준으로 스킬을 배울 순서에는 중복되는 값 즉, CCBDD와 같은 값이 없기 때문에                // C, CB, CBD 순서 중 옳은 것이 있다면                // answer 개수 추가                if (skill.substring(0, i).equals(st)) {                    answer++;                }            }        }        return answer;    }}