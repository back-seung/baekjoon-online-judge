package programmers;import java.util.*;public class Cache {    class Solution {        public int solution(int cacheSize, String[] cities) {            int answer = 0;            Queue<String> queue = new LinkedList<>();            if (cacheSize == 0) {                return cities.length * 5;            }            for (String city : cities) {                String cur = city.toUpperCase();                //hit                if (queue.contains(cur)) {                    queue.remove(cur);                    queue.add(cur);                    answer += 1;                } else {                    if (queue.size() < cacheSize){                        queue.add(cur);                    } else{                        queue.poll();                        queue.add(cur);                    }                    answer += 5;                }            }            return answer;        }    }}