package dfs_bfs;public class TargetNumber {    // 깊이 별 발생 경우의 수 : 더하거나 뺌    // 결국 모든 깊이를 탐색해야(첫번째 if 조건) 타겟 넘버가 되는지 안되는지 알 수 있음(두번째 if 조건)    // 타겟 넘버가 되는 수가 총 몇 개인지 찾아야 함    //     = 모든 깊이에 도달하기 까지 계속 두 경우의 수로 뻗어 나가야 모든 가짓수의 값을 알게 됨(else if 조건)    int answer = 0;    public int solution(int[] numbers, int target) {        DFS(0, 0, target, numbers);        return answer;    }    public void DFS(int sum, int depth, int target, int[] numbers) {        if (depth == numbers.length) {            if (sum == target) {                answer++;            }        } else if (depth < numbers.length) {            DFS(sum + numbers[depth], depth + 1, target, numbers);            DFS(sum - numbers[depth], depth + 1, target, numbers);        }    }}